---
name: purple-action-board
description: Purple Sys 的行动判断层 skill。用于基于 daily、weekly、project logs、student logs 和 meeting/material notes 生成或更新 AI 行动面板，判断现在更该做什么、哪些事情在等待条件、哪些可能已经完成，以及哪些线程已经长期未推进。只在行动判断场景触发，不用于普通 periodic 总结或 daily 归档。
---

# purple-action-board

## 概述

`purple-action-board` 是 Purple Sys 之上的“行动判断层”。

它不创建正式 task 库，不要求用户手工维护待办，也不替代 `purple-periodic`。它的职责是：

- 从 Purple Sys 的事实层提炼行动线程
- 在用户需要时判断“现在更该做什么”
- 区分“推进中”“等待条件”“待确认完成”“可忽略”
- 把结果写入固定页面 `00 Dashboard/AI 行动面板.md`

当用户想知道“下一步该做什么”“哪些事情卡住了”“最近哪些重要事项没推进”“更新行动面板”时，应使用本 skill。

## 核心规则

- 这是按需分析系统，不是主动提醒系统。
- 不接 TickTick，不创建正式任务库，不要求用户手工维护一套 task。
- 不把 daily 中每个 bullet 直接变成任务；面板按“线程”组织，而不是按单条记录组织。
- 默认不扫描整个 vault，必须控制来源范围。
- 重要但暂时推进不了的事项，放入 `等待条件`，不要误判为普通拖延。
- 用户的判断应写回行动面板本身，而不是只停留在对话中。
- 更新已有行动面板前，只对少量关键且不确定的线程提问，不逐条盘问全部内容。

## 触发范围

以下场景触发：

- “我接下来该做什么”
- “更新行动面板”
- “最近哪些重要事情没推进”
- “帮我判断哪些线程更紧急”
- “基于 Purple Sys 给我下一步建议”
- “有哪些重要但暂停的事别忘了”

以下场景不触发：

- 普通 daily 总结
- weekly / monthly 常规汇总
- 单纯归档 daily 到 log

普通 periodic 工作流仍由 [$purple-periodic](/Users/zydeng/.codex/skills/my-skills/purple-periodic/SKILL.md) 处理。

## 默认输出

- 固定页面：`00 Dashboard/AI 行动面板.md`
- 默认更新方式：整体重算覆盖
- 旧面板只作为参考读取，不做局部拼补

行动面板的固定结构与字段契约见 `references/board-contract.md`。

## 信息读取规则

默认按四层读取，而不是只看增量：

1. `变化层`
   - 上次行动面板更新之后新增的 daily / weekly / log
2. `短期背景层`
   - 最近 14 天的 periodic notes
   - 与其明确相关的 project / student / admin logs
3. `中期背景层`
   - 最近 1 个月的 weekly notes
   - 关键 logs
4. `长周期回溯层`
   - 默认不读
   - 用户明确要求时，扩展到最近 3 个月

优先级：

- daily：原始推进与当天事实
- weekly：阶段性判断、阻塞和重点方向
- project/student/admin logs：线程的长期状态
- material / meeting：只在反复出现或提供关键决策时读取

## 线程提取规则

面板按“线程”组织，而不是按单条 bullet 组织。

线程示例：

- `Paper 3 收尾`
- `二维床 / CFB 合同与采购`
- `泰格开题准备`
- `刘博异常压降排查`
- `年度预算`
- `某学生当前方向收束`

提取规则：

- 有现成 project/log 时，优先归到对应线程。
- 没有 project/log，但近期反复出现且有明确下一步时，也允许进入面板。
- 只出现一次、没有后续动作的碎片，不进入面板。
- 不要因为一次短期主题，就自动新建 project 或 log。

## 更新前交互机制

每次更新已有行动面板前：

1. 先读取旧面板。
2. 再读取新增事实和近期背景。
3. 只挑少量关键且不确定的线程提问。
4. 根据用户回答重算新版面板。

默认只问三类问题：

- 这件事算完成了吗？
- 这件事是暂缓 / 等条件，还是还应继续推进？
- 这件事还重要吗？

不要把面板更新变成大问卷。

## 行动面板状态使用原则

固定状态只有 4 类：

- `推进中`
- `等待条件`
- `待确认完成`
- `可忽略`

判断原则：

- `推进中`：近期仍在推进，或现在值得继续推进
- `等待条件`：重要，但当前受外部依赖、时机、资源或主观判断限制，短期无法推进
- `待确认完成`：从记录看大概率已完成，但信息不足以完全确认
- `可忽略`：不再重要、已失效或暂时不值得占用注意力

如果用户明确说“这件事最近做不了，但别忘了”，应放入 `等待条件`，以后每次更新仍继续显示。

## 用户判断的落点

用户对线程的判断默认写回行动面板本身，而不是分散写回原始 logs。

这样下一次更新时，可以直接读取上一次判断，例如：

- 该线程上次被标成 `等待条件`
- 该线程上次被判断为“已基本完成，待确认”
- 该线程上次被降级为 `可忽略`

## 执行方式

当用户在 live vault 中工作时，优先使用 `obsidian-cli` 读取内容。

典型流程：

1. 先读取现有行动面板；如果不存在，则按首次生成处理。
2. 按四层规则读取所需来源。
3. 提取线程，而不是罗列文件。
4. 在必要时向用户确认少量关键状态。
5. 按 `references/board-contract.md` 的固定结构整体重写 `00 Dashboard/AI 行动面板.md`。

## 测试与验收

更新或使用本 skill 时，至少应能稳定识别出以下类型的线程：

- `Paper 3`
- `二维床 / CFB`
- `泰格开题`
- `刘博异常压降`
- `预算`

并将其放入合理区块，而不是简单复述 recent daily。
